<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.QuanTieCommentMapper">
    <!-- 保持原有resultMap定义 -->
    <resultMap id="baseResultMap" type="com.example.entity.dto.QuanTieComment">
        <id property="id" column="id" />
        <result property="tieId" column="tie_id" />
        <result property="userId" column="user_id" />
        <result property="firstLevelCommonId" column="first_level_common_id" />
        <result property="username" column="username" />
        <result property="content" column="content" />
        <result property="replyTo" column="reply_to" />
        <result property="status" column="status" />
        <result property="replyToUserId" column="reply_to_user_id" />
        <result property="replyToUsername" column="reply_to_username" />
        <result property="likes" column="likes" />
        <result property="createdAt" column="created_at" />
    </resultMap>

    <select id="getPreviewRepliesForComments" resultMap="baseResultMap">
        SELECT * FROM (
            SELECT
                *, ROW_NUMBER() OVER (
                    PARTITION BY first_level_common_id 
                    ORDER BY created_at DESC
                ) AS rn
            FROM quan_tie_comments
            WHERE first_level_common_id IN
            <foreach collection="commentIds" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
            AND is_deleted = 0
        ) t WHERE t.rn &lt;= #{count}
    </select>
</mapper>
